name: Deploy to Lab

on:
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'main.py'
      - 'middleware_*.py'
      - '.github/workflows/build-and-push.yml'
  workflow_dispatch:

jobs:
  deploy:
    uses: crt-platform/infrastructure/.github/workflows/_deploy-docker-ecs.yml@71cbe8b171d0b8535db29af6c7aa51943f11a2b9
    with:
      service_name: crt-creative-studio
      repo: crt-platform/vertex-ai-creative-studio
    secrets: inherit
